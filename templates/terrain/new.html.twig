{% extends 'base.html.twig' %}

{% block content %}
    <div class="container">
        <h1 class="my-4">Créer un nouveau terrain</h1>
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card">
                    <div class="card-body">
                        {% include('terrain/_form.html.twig') %}
                    </div>
                </div>
            </div>
        </div>
        <div class="row mt-3">
            <div class="col">
                <a href="{{ path('app_terrain_index') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
