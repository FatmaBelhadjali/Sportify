{% extends 'base.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">Edit Terrain</div>
                    <div class="card-body">
                        {% block body %}
                            <h1>Edit Terrain</h1>

                            {{ include('terrain/_form.html.twig', {'button_label': 'Update'}) }}

                            <a href="{{ path('app_terrain_index') }}" class="btn btn-secondary">Back to list</a>

                            {{ include('terrain/_delete_form.html.twig') }}
                        {% endblock %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
