{% extends 'base.html.twig' %}

{% block content %}
    <div class="container">
        <h1 class="my-4">Modifier le sport</h1>
        <div class="row">
            <div class="col-md-6">
                {{ include('sport/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
        <div class="row mt-3">
            <div class="col">
                <a href="{{ path('app_sport_index') }}" class="btn btn-secondary">Retour à la liste</a>
                {{ include('sport/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
